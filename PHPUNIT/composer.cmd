#After creating composer.json, this command was executed.
 	composer.json is saying that at phpunit version should be at least 5.6
	All the files are coming from 'app' directory.

	composer dump-autoload -o
	
#All tests must have file name xxxTest.php.

#phpunit configuration such as coloration, verbosity is added to phpunit.xml

#File structure is very critical.
      /vendor/composer
